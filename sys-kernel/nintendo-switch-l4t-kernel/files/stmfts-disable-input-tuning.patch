From 97a226602758be47bb0b939346a2b5ab7d25d409 Mon Sep 17 00:00:00 2001
From: Billy Laws <blaws05@gmail.com>
Date: Wed, 19 Jun 2019 18:23:55 +0100
Subject: [PATCH] input: stmfts: Disable input tuning

Fixes a bug where the touchscreen detects inputs from like 1cm away with a pencil
---
 drivers/input/touchscreen/stmfts.c | 12 ------------
 1 file changed, 12 deletions(-)

diff --git a/drivers/input/touchscreen/stmfts.c b/drivers/input/touchscreen/stmfts.c
index d876884d50a44..dd2c43795e5ba 100644
--- a/drivers/input/touchscreen/stmfts.c
+++ b/drivers/input/touchscreen/stmfts.c
@@ -637,18 +637,6 @@ static int stmfts_power_on(struct stmfts_data *sdata)
 		sdata->no_sleep_support = true;
 	}
 
-	/* optional tuning */
-	err = stmfts_command(sdata, STMFTS_MS_CX_TUNING);
-	if (err)
-		dev_warn(&sdata->client->dev,
-			 "failed to perform mutual auto tune: %d\n", err);
-
-	/* optional tuning */
-	err = stmfts_command(sdata, STMFTS_SS_CX_TUNING);
-	if (err)
-		dev_warn(&sdata->client->dev,
-			 "failed to perform self auto tune: %d\n", err);
-
 	err = stmfts_command(sdata, STMFTS_FULL_FORCE_CALIBRATION);
 	if (err) {
 		dev_err(&sdata->client->dev,
-- 
2.21.0

